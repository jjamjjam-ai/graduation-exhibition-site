# graduation-exhibition-site

Static site starter for a graduation exhibition project.

## Structure

- `index.html` — landing page
- `styles.css` — base styling
- `assets/images/` — static image assets

## Deploy

Upload the project files to an S3 bucket configured for static hosting.
